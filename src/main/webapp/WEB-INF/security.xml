<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans
                 http://www.springframework.org/schema/beans/spring-beans.xsd
                 http://www.springframework.org/schema/security
                 http://www.springframework.org/schema/security/spring-security-4.0.xsd">

    <beans:bean id="userService" class="com.resources.auth.database.Users.UserDAO">
        <beans:property name="sessionFactory" ref="sessionFactory"/>
    </beans:bean>
    <beans:bean id="userServiceSec" class="com.resources.auth.database.Users.UserDAOSec" >
        <beans:property name="userService" ref="userService" />
    </beans:bean>
    <beans:bean id="eventListenerBean" class="com.resources.auth.AppInit" />

    <!-- enable use-expressions -->
    <http use-expressions="true">
        <intercept-url pattern="/tableRegistered*" access="hasRole('ROLE_ADMIN')" />
        <intercept-url pattern="/servers*" access="hasRole('ROLE_ADMIN')" />
        <intercept-url pattern="/userServers*" access="hasRole('ROLE_USER')" />
        <form-login
                login-page="/"
                authentication-failure-url="/logErr"
                username-parameter="username"
                password-parameter="password"
                authentication-success-handler-ref="myAuthenticationSuccessHandler"
                />
        <csrf disabled="true" />
    </http>


    <beans:bean id="myAuthenticationSuccessHandler"
                class="com.resources.auth.Security.standardAuthenticationSuccessHandler" />

    <authentication-manager>
        <authentication-provider user-service-ref="userServiceSec" >
            <password-encoder ref="passwordEncoder" />
        </authentication-provider>
    </authentication-manager>

    <beans:bean id="passwordEncoder" class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder">
        <beans:constructor-arg name="strength" value="10" />
    </beans:bean>

</beans:beans>
